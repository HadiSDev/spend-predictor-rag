## 1. AI-owned categorization store

- [x] 1.1 Create `src/ai_api/persistence/__init__.py` and a `categorization.py`
  defining `LineCategorization(SQLModel, table=True)` with `id`, `invoice_line_id`
  (FK → `invoice_lines.id`, `ON DELETE CASCADE`, unique), nullable
  `spend_category_id` (FK → `spend_categories.id`), predicted `level_1/2/3`,
  `account_code`, `account_name`, `confidence`, `rationale`, `gt_level_1/2/3`,
  `gt_account_code`, `created_at` — no ORM `Relationship()` into domain classes.
- [x] 1.2 Ensure the model is registered in shared `SQLModel.metadata` for
  `create_all` by importing `ai_api.persistence` from the runner. Do NOT import it
  into `web_api`'s Alembic `env.py` (that would cross the `web_api → ai_api`
  boundary); migrations are hand-authored (not autogenerated), so `target_metadata`
  completeness is unnecessary and the live table is created by migration 0010.

## 2. Slim the domain models

- [x] 2.1 Remove from `InvoiceLine` (`src/web_api/db/models/invoice_line.py`):
  `level_1/2/3`, `account_code`, `account_name`, `confidence`, `rationale`,
  `gt_level_1/2/3`, `gt_account_code`, and `line_erp_id`. Keep `spend_category_id`,
  its relationship, the raw fields, and `status`.
- [x] 2.2 Confirm no domain model references the removed fields or the AI store
  (`web_api` must not import `ai_api.persistence`).

## 3. Rewire the sync pipeline

- [x] 3.1 In `runner._categorize_pending`: upsert a `LineCategorization` row per
  line (prediction + evidence + ground truth); set `InvoiceLine.spend_category_id`
  only when the match resolves to a real `SpendCategory` (else leave null); keep
  line/invoice `status` transitions on the domain model.
- [x] 3.2 In `runner._build_summary`: compute `spend_by_level_2` from the AI
  store's predicted `level_2` joined to the line amount, not from `InvoiceLine`
  columns.

## 4. API contract

- [x] 4.1 In `schemas.py` `InvoiceLineRead`: remove `level_1/2/3`, `account_code`,
  `account_name`, `confidence`, `rationale`; ensure `spend_category_id` is present.

## 5. Migration

- [x] 5.1 Add migration `0010_ai_categorization_store` (down_revision
  `0009_line_spend_category`, id ≤32 chars): create `line_categorizations` and
  drop the removed columns from `invoice_lines`; downgrade re-adds the columns and
  drops the table.
- [x] 5.2 Apply against the live DB on port 5433 and verify the table exists and
  the columns are gone.

## 6. Tests

- [x] 6.1 Update `tests/test_sync_runner.py`: assert removed columns are absent
  from `InvoiceLine`; assert a `LineCategorization` row is written per categorized
  line with the predicted snapshot + ground truth; assert `spend_by_level_2` still
  reflects the predicted `level_2`.
- [x] 6.2 Update `tests/web_api/` fixtures/assertions that set or read the removed
  `InvoiceLine` columns (e.g. `conftest.py`, invoice-review tests).
- [x] 6.3 Run the full suite (`uv run pytest`) green.

## 7. Strip vestigial categorization columns off ErpEntry

- [x] 7.1 Remove from `ErpEntry` (`src/web_api/db/models/erp_entry.py`):
  `level_1/2/3`, `account_code`, `account_name`, `confidence`, `rationale`,
  `gt_level_1/2/3`, `gt_account_code` (entries are never categorized — always
  NULL; no AI store for entries). Keep `erp_account_id`, `status`, raw fields.
- [x] 7.2 Remove `account_code`/`account_name` from `ErpEntryRead` in `schemas.py`.
- [x] 7.3 Add migration `0011_drop_entry_categorization` (down_revision
  `0010_ai_categorization_store`): drop the columns; downgrade re-adds them. Apply
  on port 5433 and verify.
- [x] 7.4 Update tests: `test_sync_runner` (assert entry categorization columns
  absent) and `tests/web_api/test_erp_entries.py` (drop the `gt_account_code`
  fixture kwarg). Full suite green.
