"""AI service: LLM/RAG workflows for spend categorization, redundancy detection,
and savings recommendations.

Depends on the ``web_api`` business-domain package (ORM models, DB session, ERP
connectors); the domain never imports back into ``ai_api``.
"""
